rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Users can only read/write their own beats
    match /users/{userId}/beats/{beatId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
